# Solar Scout - Lead Generation Dependencies
# Install with: pip3 install -r requirements.txt

# Core
requests>=2.31.0
beautifulsoup4>=4.12.0

# DuckDuckGo search (no API key required)
duckduckgo-search>=7.0.0

# Optional: for better HTML parsing
lxml>=4.9.0

# Optional: for faster HTTP (alternative to requests)
# httpx>=0.25.0

# Optional: for rotating proxies (if needed)
# requests-file>=1.5.1
